# Copy to .env.local for local development

# Backend API URL — leave blank to use the Vite dev proxy (localhost:4000)
# Set to your Railway URL in production: https://your-api.up.railway.app/api
VITE_API_URL=
